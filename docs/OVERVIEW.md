# 概要 / 現状（2025-08-24）

- Cloud Run: `squadbeyond-scraper`（asia-northeast1） – 稼働中
- Artifact Registry: `cloud-run`（asia-northeast1） – Reader 付与済
- Secrets: `SQUAD_THEMARCH01_EMAIL` / `SQUAD_THEMARCH01_PASSWORD` – `versions/latest` 運用
- Bridge: `BRIDGE_DIR=/app/bridge`、`accounts.yaml` に `squad_themarch01`、active レポート2件
- ログタグ: `[login2] [team2] [nav2] [diag]`（出力実装は要確認）
